// Allowed image MIME types
export const IMAGE_ALLOWED_TYPES = [
  'image/jpeg',
  'image/jpg',
  'image/png',
  'image/webp',
];

// Default file size (2MB)
export const DEFAULT_FILE_SIZE = 2 * 1024 * 1024;
